import { Record } from "../lib/api";
export default function RecordsTable({ records, onEdit, onDelete }: { records: Record[]; onEdit: (record: Record) => void; onDelete: (id: string) => void }) { return <div className="table-wrap"><table><thead><tr><th>Record name</th><th>Type</th><th>Value / Route traffic to</th><th>TTL</th><th /></tr></thead><tbody>{records.map(record => <tr key={record.id}><td>{record.name}<span className="muted block">{record.description || ""}</span></td><td><span className="record-type">{record.type}</span></td><td className="value">{record.value}</td><td>{record.ttl === 0 ? "Alias" : `${record.ttl}s`}</td><td className="actions"><button className="text-button" onClick={() => onEdit(record)}>Edit</button><button className="text-button danger" onClick={() => onDelete(record.id)}>Delete</button></td></tr>)}{!records.length && <tr><td colSpan={5} className="empty">No records yet. Add one to start routing traffic.</td></tr>}</tbody></table></div>; }
